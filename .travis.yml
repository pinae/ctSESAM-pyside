language: python
python:
  - "3.4"
  - "3.5.0b3"
  - "nightly"
# command to install dependencies
addons:
  apt:
    packages:
    - qt5-default
install: "pip install -r requirements.txt"
# command to run tests
script: nosetests